name: Test My Action

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: edgedb42/setup-edgedb@9fab8b00c263ade78dd5c9d447d568a90910d391
        with:
          exampleInput: "Amorin"
